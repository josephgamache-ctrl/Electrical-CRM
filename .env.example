# MA Electrical Inventory - Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to version control - it's in .gitignore

# ===========================================
# REQUIRED - These must be set
# ===========================================

# Database password (use a strong password)
DB_PASSWORD=your_secure_database_password_here

# JWT Secret Key (generate with: openssl rand -base64 32)
SECRET_KEY=your_jwt_secret_key_here_minimum_32_characters

# ===========================================
# OPTIONAL - These have defaults
# ===========================================

# Token expiration in minutes (default: 60)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ===========================================
# DEPLOYMENT CONFIGURATION
# ===========================================

# Frontend Configuration
API_URL=http://your-droplet-ip:8001

# Production Ports
FRONTEND_PORT=3001
BACKEND_PORT=8001

# For Production with HTTPS (Digital Ocean)
# API_URL=https://your-domain.com/api
# FRONTEND_PORT=80
